// Keep users in memory as this is just a simple sample

class User {
    constructor(id, nickname) {
      this.id = id;
      this.nickname = nickname;
    }
  }
  
  module.exports = User;
  